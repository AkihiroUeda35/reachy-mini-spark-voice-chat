[project]
name = "spark-voice-chat"
version = "0.1.0"
description = "Local Qwen + CosyVoice workspace utilities"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "langchain-core>=1.3.2",
    "langchain-deepseek>=1.0.1",
]

[tool.uv]
package = false